tags html:
nível bloco(um por linha)
nível line(mais de um por linha)
divisões por section
css:
class id para os selectors
prioridade por último ou por n° de entidades(elements,class,id)
pseudo-class para clique
css reset margin
box model:
mantém a proporção correta dos elementos
